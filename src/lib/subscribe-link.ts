/**
 * Where the parent dashboard's subscribe CTA points. This app never embeds
 * a Shopify checkout URL directly or owns pricing content — that lives in
 * the landingpage-densha repo (draft/pricing.html today; promoted to the
 * site root, with the placeholder Shopify checkout link filled in, once a
 * paid plan is ready to sell). Update this constant to the real path only
 * once that promotion has happened; until then it's a dead link by design,
 * not a bug.
 */
export const SUBSCRIBE_HREF = "https://kanji-ai.jp/pricing.html";
